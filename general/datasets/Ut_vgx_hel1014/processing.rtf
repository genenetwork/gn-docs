<p>Standard 2z+8 of log2 data. RMA data were log2 transformed (adding an olffset of 1 to avoid negative values). Variance was stablized (i.e. each array was converted to a set of Z scores). Z scores were then multipled by 2. &nbsp;Mean Z &nbsp;was then shifted from 0 to to&nbsp;8 units per array.</p>
